These are the notebook forms of the main framework.

ranking.ipynb creates all the ranking files to run generation. K is defaulted to 5, change in code for adjustment. Handles contriever and bm25. It will create all 6 files for each 3 task type for designiated split and ranking method.